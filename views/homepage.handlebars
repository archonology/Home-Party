<div class="jumbotron">
  <h1 class="display-4">Your Dream Home Awaits</h1>
  <p class="lead">Looking for that perfect home?</p>
  <p class="lead">Home Party is a place to invite your family and friends; a place where you can
    keep track of all your favorite homes... and everything you could put in them.</p>
  <hr class="my-4">
  <p></p>
</div>
<br>
<br>
<h2>All Saved Homes:</h2>
<br>
<section class="d-flex flex-wrap justify-content-center">
  {{#each homes as |home|}}
  <div class="home-card p-1 ml-3 mb-5" style="width: 40rem;">
    <div class="card-body p-3">
      <a href="homes/{{home.id}}"><h5 class="card-title">{{home.address}}</h5></a>
       <h6 class="card-subtitle mb-2 text-dark">Added on {{home.date_saved}} || <strong>${{home.price}}</strong></h6>
      <p class="card-text">{{home.title}} -- {{home.user.username}}</p>
      <a href="{{home.link}}"><button id="gotohome-link" class="btn card-link">Visit the Link</button></a>
      {{!-- <img src="../public/images/icons8-house-48.png" alt="house icon"> --}}
    </div>
  </div>
  {{/each}}
  <br>
  <br>
</section>
<h2>All Saved Decor:</h2>
<section class="d-flex flex-wrap justify-content-center">
  {{#each decors as |decor|}}
  <br />
  <div class="decor-card p-1 ml-3 mt-3" style="width: 40rem;">
    <div class="card-body p-3">
      <h5 class="card-title">{{decor.product_name}}</h5>
      <h6 class="card-subtitle mb-2 text-dark">Added on
        {{decor.date_saved}} || <strong>${{decor.price}}</strong> </h6>
      <p class="card-text">{{decor.description}} -- {{decor.user.username}}</p>
        <a href="{{decor.link}}"><button id="gotohome-link" class="btn card-link">Visit the Link</button></a>
      </div>
    </div>
  </div>
  {{/each}}
</section>